@model GestionQR.Models.Reclamaciones

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Reclamaciones</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

        @Html.HiddenFor(model => model.Cliente)

        @Html.HiddenFor(model => model.Usuario_Reclamo)

        @Html.HiddenFor(model => model.Tipo_Reclamacion)

        @Html.HiddenFor(model => model.Departamento_Reclamacion)

        @Html.HiddenFor(model => model.Encargado_Reclamacion)

        @Html.HiddenFor(model => model.Fecha_Reclamacion)

        @Html.HiddenFor(model => model.Hora_Reclamacion)

        @Html.HiddenFor(model => model.Departamento_Respuesta)

        @Html.HiddenFor(model => model.Fecha_Respuesta)

        @Html.HiddenFor(model => model.Estatus_Reclamacion)

        @Html.HiddenFor(model => model.Comentarios_Reclamaciones)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
