@model GestionQR.Models.Usuarios_de_quejas

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Usuarios_de_quejas</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

        @Html.HiddenFor(model => model.Usuario_quejas)

        @Html.HiddenFor(model => model.Clave)

        @Html.HiddenFor(model => model.Departamento_Queja)

        @Html.HiddenFor(model => model.Fecha_Queja)

        @Html.HiddenFor(model => model.Hora_Queja)

        @Html.HiddenFor(model => model.Rol)

        @Html.HiddenFor(model => model.Puesto)

        @Html.HiddenFor(model => model.Cliente)

        @Html.HiddenFor(model => model.Producto)

        @Html.HiddenFor(model => model.Estado)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
